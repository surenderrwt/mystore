<%= form_with(model: state, local: true) do |form| %>
  <% if state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(state.errors.count, "error") %> prohibited this state from being saved:</h2>

      <ul>
      <% state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :country_id  %>
    <%#= form.text_field :country_id, id: :state_country_id %>
    <%= form.select :country_id, @countries.collect { |cunt| [cunt.name, cunt.id] } %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :state_name %>
  </div>

  

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
